import React from 'react';
import styles from "./Header.css";

const Header = () => 
  (<header className={styles.Header}>
    <h1 className={styles.Header__title}>Super Squad</h1>
  </header>);

export default Header;
